<resources>
    <style name="Theme.Maksut" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Poistetaan oletus-ActionBar -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- muu teema, esim. värit -->
        <!-- Pääväri (vaikuttaa toolbar‐väriin, jos toolbar on) -->
        <item name="colorPrimary">@color/brand_primary</item>
        <!-- Tummempi variantti, jota yleensä käytetään status‐barissa -->
        <item name="colorPrimaryDark">@color/brand_primary_dark</item>

        <!-- Jos haluat varmistaa, että status‐bar on samansävyinen: -->
        <item name="android:statusBarColor">@color/brand_primary_dark</item>
        <!-- Navigaatiopalkki (jos on näkyvissä): -->
        <item name="android:navigationBarColor">@color/brand_primary_dark</item>
    </style>

    <style name="SpinnerStyle" parent="Widget.AppCompat.Spinner.Underlined">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:popupBackground">#294D73</item>
    </style>
</resources>
